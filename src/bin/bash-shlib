#!/usr/bin/env bash

source="$1"

shift

exec /bin/bash -c "

if [ -e /etc/shlib ]; then
     . /etc/shlib || {
         echo \"Unsatisfied dependency. Please install 'kal-shlib-core'.\" >&2
         exit 1
     }
fi

. \"$source\"

" "$source" "$@"
